# 2026-06-22 — UI 개선 · 캘린더 날짜 상세 패널

## 작업 개요

My 탭 로그아웃 버튼 위치 이동, 홈 T 아바타 제거,  
캘린더 날짜 클릭 시 일정 상세 패널 표시 기능 추가.

---

## My 탭 로그아웃 버튼 이동 — `src/pages/My/index.jsx`

### 변경 전
헤더 우측에 `이름 ✕` 형태의 인라인 버튼

### 변경 후
시험 관리 섹션 최하단에 독립 버튼으로 이동

```
[ 로그아웃 ]  ← 로그인 상태에서만 표시, 하단 고정
```

- 기존 헤더 로그아웃 버튼 제거
- 하단에 전체 너비 버튼 추가 (`border: 1.5px solid #EDE8FF`)

---

## 홈 T 아바타 제거 — `src/pages/Home/index.jsx`

우상단에 있던 `T` 텍스트 아바타(의미 없는 장식 요소) 완전 제거.

---

## 캘린더 날짜 상세 패널 — `src/pages/Schedule/index.jsx`

### 동작

Apple Calendar 스타일 참고.  
날짜를 탭하면 캘린더 하단에 해당 날의 일정 목록이 표시됨.

```
[캘린더 그리드]
  ↓ 날짜 탭
┌─────────────────────────────────┐
│ 6월 22일 (월)              ✕   │
├─────────────────────────────────┤
│ ▌ ADsP            IT·Data  D-3  │
│ ▌ SQLD            IT·Data  D-3  │
└─────────────────────────────────┘
```

### 구현 내용

- `CalendarView`에 `selectedDay` 상태 추가
- 날짜 셀 클릭 → `selectedDay` 토글 (같은 날 재탭 시 닫힘)
- 선택된 날짜: 보라색 배경(`#EDE8FF`) + 날짜 숫자 보라색·볼드
- 월 이동 시 `useEffect`로 선택 자동 해제
- 패널 내용: 시험명 / 일정 유형 / 카테고리 뱃지 / D-Day 뱃지
- 일정 없는 날: "일정이 없는 날이에요" 안내

### catFilter 연동

`CalendarView`에 `catFilter` prop 추가.  
카테고리 필터 적용 상태가 캘린더 점(dot) 표시와 상세 패널 모두에 반영됨.

### 변경 파일
- `src/pages/Schedule/index.jsx`
  - `useEffect` import 추가
  - `CalendarView` props: `year, month` → `year, month, catFilter`
  - `selectedDay` 상태 및 상세 패널 UI 추가
  - 3개 이상 일정 있는 날: 회색 점 추가로 truncation 표시

---

## 배포 이력

| 커밋 | 내용 |
|---|---|
| `feat: My 탭 로그아웃 버튼 하단 이동, 홈 T 아바타 제거` | UI 정리 |
| `feat: 캘린더 날짜 클릭 시 일정 상세 패널 표시` | 캘린더 인터랙션 |

---

---

## 하단 여유 공간 웹/앱 통일 — `src/index.css` + 전체 페이지

### 문제

`BottomNav`는 `paddingBottom: env(safe-area-inset-bottom)`을 적용해  
실제 높이 = `60px + safe-area`인데, 각 페이지는 `paddingBottom: 80`으로 고정.

- **웹 브라우저**: safe-area = 0 → 탭바 60px, 페이지 패딩 80px → 하단 여유 20px ✓
- **앱 (iPhone PWA)**: safe-area ≈ 34px → 탭바 94px, 페이지 패딩 80px → 콘텐츠 잘림 ✗

### 수정

`src/index.css`에 CSS 변수 추가:

```css
:root {
  --page-pb: calc(80px + env(safe-area-inset-bottom));
}
```

전체 5개 페이지(`Home`, `Schedule`, `My`, `Info`, `Quiz`)의  
`paddingBottom: 80` → `paddingBottom: 'var(--page-pb)'` 일괄 교체.

| | 웹 | 앱 (수정 전) | 앱 (수정 후) |
|---|---|---|---|
| 탭바 높이 | 60px | 94px | 94px |
| 페이지 하단 패딩 | 80px | 80px | 114px |
| 탭바 위 여유 공간 | 20px | ❌ 잘림 | 20px ✓ |

---

---

## 전산회계 2026년 실제 시험 일정 추가 — `src/data/exams.js`

출처: https://license.kacpta.or.kr/m/info/info_diary.aspx (한국세무사회)

| 회차 | 접수 기간 | 시험일 | 합격 발표 |
|---|---|---|---|
| 124회 | 01.02 ~ 01.08 | 01.31 | 02.26 |
| 125회 | 03.05 ~ 03.11 | 04.04 | 04.23 |
| 126회 | 04.30 ~ 05.07 | 06.06 | 06.25 |
| 127회 | 07.02 ~ 07.08 | 08.01 | 08.20 |
| 128회 | 08.27 ~ 09.02 | 10.03 | 10.29 |
| 129회 | 11.05 ~ 11.11 | 12.05 | 12.24 |

ID 패턴: `ca-{회차}-{rs|re|ex|r}`

---

## 투자자산운용사 2026년 실제 시험 일정 추가 — `src/data/exams.js`

출처: license.kofia.or.kr (금융투자협회)

| 회차 | 접수 기간 | 시험일 | 합격 발표 |
|---|---|---|---|
| 44회 | 25.12.22 ~ 25.12.26 | 01.18 | 01.29 |
| 45회 | 26.04.13 ~ 26.04.17 | 05.10 | 05.21 |
| 46회 | 07.27 ~ 07.31 | 08.23 | 09.03 |
| 47회 | 10.12 ~ 10.16 | 11.08 | 11.19 |

ID 패턴: `im-{회차}-{rs|re|ex|r}`

---

## 타임라인 뷰 — 미래 회차 기준 표시 수정 — `src/pages/Schedule/index.jsx`

### 문제

`schedules.find(sc => sc.type === step.type)`가 데이터의 첫 번째 항목을 반환하여  
전산회계처럼 여러 회차가 있는 경우 이미 지난 회차 일정이 표시되었음.

### 수정

`getRoundEntry(type)` 헬퍼 함수 도입:

```js
// 가장 가까운 미래 시험일 찾기
const futureExams = schedules
  .filter((s) => s.type === 'exam' && new Date(s.date) >= today)
  .sort((a, b) => new Date(a.date) - new Date(b.date));
const nextExamEntry = futureExams[0];   // 다음 시험
const afterExamEntry = futureExams[1];  // 다다음 시험 (next_exam 칸)

const getRoundEntry = (type) => {
  if (type === 'exam')      return nextExamEntry || null;
  if (type === 'next_exam') return afterExamEntry || null;
  // 접수 시작/마감·취소 마감: 다음 시험일 이전 항목 중 가장 최근 것
  return schedules
    .filter((s) => s.type === type && new Date(s.date) <= new Date(nextExamEntry.date))
    .sort((a, b) => new Date(b.date) - new Date(a.date))[0] || null;
};
```

- 헤더 D-Day 배지도 `nextExamEntry` 기준으로 표시
- `isPast` 판정도 `today` 기준으로 정확하게 수정

---

## 배포 이력 (추가)

| 커밋 | 내용 |
|---|---|
| `feat: 전산회계 2026년 실제 시험 일정 추가 (124~129회)` | 일정 데이터 |
| `feat: 투자자산운용사 2026년 실제 시험 일정 추가 (44~47회)` | 일정 데이터 |
| `fix: 타임라인 뷰 - 가장 가까운 미래 회차 일정만 표시` | 버그 수정 |

---

## 다음 할 일

- [ ] TOEIC·OPIc 실제 시험 일정 추가
- [ ] 퀴즈 풀이 결과 저장 및 히스토리 (로그인 후)
- [ ] 푸시 알림 (D-30 / D-7 / D-1)
